[package]
name = "gnome-vault"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
gtk = { package = "gtk4", version = "0.11" }
adw = { package = "libadwaita", version = "0.9", features = ["v1_5"] }
gpgme = "0.11"

[build-dependencies]
glib-build-tools = "0.22"
